import React, { FC } from 'react';

const Mutations: FC = () => (
  <div style={{
    position: 'absolute',
    zIndex: -1,
    top: 0,
    width: '100%',
    height: '100vh',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  }}
  >
    <p>
      Mutations
    </p>
  </div>
);

export default Mutations;
